import { DisableFunctionKeysDirective } from './disable-function-keys.directive';

describe('DisableFunctionKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableFunctionKeysDirective();
    expect(directive).toBeTruthy();
  });
});
